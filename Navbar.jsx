import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
    title: 'PeerFetch - Engineering Student Networking Platform',
    description: 'Connect with seniors and juniors. Find mentors and mentees in your engineering college.',
    keywords: 'engineering, students, mentorship, networking, college',
}

export default function RootLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <html lang="en">
            <body className={inter.className}>{children}</body>
        </html>
    )
}
